git init
git status
git config --global user.name "Korol Roman"
git config --global user.email "rommypost@gmail.com"
git add index.php
git commit -m 'в коммите указать что было сделано и для чего'
git log
  - вниз enter
  - выйти q
git config --global alias.tree "log --oneline --decorate --all --graph"
git tree
git diff
git checkout <hash>
git checkout master
git branch
 master -неприкосновенная ветка в которую не должны просто так попадать коммиты
git branch my-new-branch
'dummy-бессмысленный комментарий'
